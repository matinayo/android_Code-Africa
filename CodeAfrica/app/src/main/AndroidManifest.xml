<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.adu.codeafrica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".User.QuizActivity" />
        <activity android:name=".User.LanguageActivity" />
        <activity android:name=".User.LearnActivity" />
        <activity android:name=".User.WebDevelopmentActivity" />
        <activity android:name=".User.AllCategories" />
        <activity android:name=".User.UserDashboard" />
        <activity android:name=".Common.OnBoarding" />
        <activity android:name=".Common.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>